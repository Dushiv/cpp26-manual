{
  "id": "m9",
  "moduleNumber": 9,
  "title": "Numbers and low-level",
  "significance": "special",
  "prerequisites": [
    "m8"
  ],
  "lessons": [
    {
      "id": "m9-l1",
      "title": "<stdfloat>: Fixed-Width Floating-Point Types",
      "prerequisites": ["m8-l2"],
      "background": null,
      "motivation": "The types `float`, `double`, and `long double` have existed in C++ since its C heritage, but they carry a fundamental flaw: the standard never guaranteed their exact sizes in bytes. On most x86 platforms `float` is 32-bit IEEE 754 and `double` is 64-bit, but this is a de-facto norm rather than a language requirement. On certain embedded and DSP platforms the picture is different. For code that needs exact IEEE 754 semantics — network protocols, file formats, machine-learning libraries — this ambiguity causes portability problems. C++23 addresses this by introducing the `<stdfloat>` header with five new fixed-width floating-point types.",
      "theory": "Before C++23, programmers who needed guaranteed precision wrote `typedef float float32_t;` — but that is merely an alias, not a new type. Two aliases for the same type cannot serve as the basis for function overloading, and the compiler cannot distinguish `float32_t` from `float`. C++23 solves this by introducing truly distinct types in the language's type system.\n\nThe `<stdfloat>` header defines up to five types: `std::float16_t` (IEEE 754 binary16, half precision), `std::float32_t` (IEEE 754 binary32, single precision — matches `float` on most platforms), `std::float64_t` (IEEE 754 binary64, double precision), `std::float128_t` (IEEE 754 binary128, quad precision — requires hardware support), and `std::bfloat16_t` (Brain Float 16, a format developed by Google/Intel). The standard only provides a type if the platform supports it; on x86-64 with GCC/Clang, all four except `float128_t` are typically available.\n\n`bfloat16_t` deserves special attention. Unlike `float16_t`, which has 5 exponent bits and 10 mantissa bits, `bfloat16_t` has the same 8 exponent bits as `float32_t` but only 7 mantissa bits. This means the same value range as `float32_t` at half the memory footprint. That is precisely why Google TPU, Intel AMX, TensorFlow, and PyTorch use bfloat16 for neural-network weights: training stability demands a wide dynamic range (to avoid overflow/underflow), not maximum mantissa precision.\n\nIn practice all new types support arithmetic operators and work with `std::numeric_limits<T>`. `std::format` and `std::print` format them correctly. Initializing from `float` literals requires an explicit cast (`static_cast`) — implicit narrowing in brace-initialization is forbidden. The key advantage over the old `typedef` approach is that these types are distinct in the type system, enabling overloaded functions that accept `float16_t` and `float32_t` as different arguments.",
      "examples": [
        {
          "id": "m9-l1-ex1",
          "title": "Sizes and precision of the new types",
          "description": "Checking the sizes of all available types from `<stdfloat>` and their precision via `std::numeric_limits`.",
          "code": "#include <stdfloat>\n#include <limits>\n#include <print>\n\nint main() {\n    std::println(\"sizeof float16_t:  {}\", sizeof(std::float16_t));\n    std::println(\"sizeof float32_t:  {}\", sizeof(std::float32_t));\n    std::println(\"sizeof float64_t:  {}\", sizeof(std::float64_t));\n    std::println(\"sizeof bfloat16_t: {}\", sizeof(std::bfloat16_t));\n\n    std::println(\"float32_t max digits10: {}\", std::numeric_limits<std::float32_t>::max_digits10);\n    std::println(\"float64_t max digits10: {}\", std::numeric_limits<std::float64_t>::max_digits10);\n    std::println(\"float16_t max digits10: {}\", std::numeric_limits<std::float16_t>::max_digits10);\n}",
          "expectedOutput": "sizeof float16_t:  2\nsizeof float32_t:  4\nsizeof float64_t:  8\nsizeof bfloat16_t: 2\nfloat32_t max digits10: 9\nfloat64_t max digits10: 17\nfloat16_t max digits10: 5",
          "outputsVerified": true,
          "compiler": "gsnapshot",
          "compilerFlags": "-std=c++23 -O2"
        },
        {
          "id": "m9-l1-ex2",
          "title": "bfloat16 for storing neural-network weights",
          "description": "A practical ML scenario: storing weights in `bfloat16_t` instead of `float32_t` halves memory usage. Arithmetic is performed via explicit cast to `float`.",
          "code": "#include <stdfloat>\n#include <vector>\n#include <print>\n\nint main() {\n    // Simulate storing ML weights in bfloat16 vs float32\n    std::vector<std::bfloat16_t> bf16_weights = {\n        static_cast<std::bfloat16_t>(0.5f),\n        static_cast<std::bfloat16_t>(-0.25f),\n        static_cast<std::bfloat16_t>(1.0f),\n        static_cast<std::bfloat16_t>(0.125f)\n    };\n\n    std::vector<std::float32_t> f32_weights = {0.5f, -0.25f, 1.0f, 0.125f};\n\n    std::println(\"bfloat16 storage: {} bytes for 4 weights\",\n        bf16_weights.size() * sizeof(std::bfloat16_t));\n    std::println(\"float32 storage:  {} bytes for 4 weights\",\n        f32_weights.size() * sizeof(std::float32_t));\n\n    // Arithmetic works — convert to float32 for computation\n    float sum = 0.0f;\n    for (auto w : bf16_weights)\n        sum += static_cast<float>(w);\n    std::println(\"sum of bf16 weights: {}\", sum);\n\n    // Type system: float16_t and float32_t are distinct types\n    auto f16 = static_cast<std::float16_t>(1.5f);\n    std::float32_t f32 = 1.5f;\n    std::println(\"float16_t == float32_t value: {}\", static_cast<float>(f16) == f32);\n}",
          "expectedOutput": "bfloat16 storage: 8 bytes for 4 weights\nfloat32 storage:  16 bytes for 4 weights\nsum of bf16 weights: 1.375\nfloat16_t == float32_t value: true",
          "outputsVerified": true,
          "compiler": "gsnapshot",
          "compilerFlags": "-std=c++23 -O2"
        }
      ],
      "outputsVerified": true
    },
    {
      "id": "m9-l2",
      "title": "`std::byteswap`, `std::to_underlying`, `std::unreachable`: Low-Level Utilities",
      "prerequisites": ["m9-l1"],
      "background": null,
      "motivation": "Before C++23, reversing the bytes of an integer meant writing `__builtin_bswap32(x)` on GCC/Clang or `_byteswap_ulong(x)` on MSVC. Marking a code path as unreachable required `__builtin_unreachable()` or `__assume(false)`. Extracting the underlying value from an `enum class` demanded the verbose `static_cast<std::underlying_type_t<Color>>(value)`. Each of these patterns solved a real problem, but with non-standard syntax: the code was non-portable and littered the codebase with platform-specific details. C++23 standardizes all three operations as clean, `constexpr`-ready functions.",
      "theory": "Historically C++ inherited from C a large number of useful low-level operations that remained outside the standard — each compiler supplied its own extensions. C++20 began the cleanup: `<bit>` gained `std::popcount`, `std::rotl`, `std::rotr`. C++23 adds three more utilities, closing the most common pain points.\n\n`std::byteswap` (header `<bit>`, P1272) reverses the byte order of any integral type. It accepts any `integral` type — `uint8_t`, `uint16_t`, `uint32_t`, `uint64_t`, `int`, and so on — and returns the same type with its bytes swapped. The function is `constexpr`, so the computation can happen at compile time. Typical use cases: converting between network byte order (big-endian protocol) and host byte order (little-endian), reading binary file formats, and certain hashing algorithms. Before C++23 the alternatives were `__builtin_bswap32` / `__builtin_bswap64` (GCC/Clang) or `_byteswap_ulong` (MSVC), or manual bitwise arithmetic.\n\n`std::unreachable` (header `<utility>`, P0627) marks a point in the code as unreachable. If execution actually reaches that point, the behavior is undefined. The compiler uses this promise for optimization: it can eliminate checks in `switch` defaults, shrink the control-flow graph, and remove dead branches. The function is especially useful after an exhaustive `switch` over an `enum` — it eliminates the \"not all paths return a value\" warning and gives the optimizer complete information. **Important:** unlike `assert()`, `std::unreachable` performs no runtime check in either debug or release builds. If the unreachability assumption is wrong, the program has silent undefined behavior. Use it only where you are absolutely certain about the invariant.\n\n`std::to_underlying` (header `<utility>`, P1682) returns an enumeration value cast to its underlying type. It works with both `enum class` and plain `enum`, and returns `std::underlying_type_t<E>` — always the exact type declared as the enum's base. This function was covered in detail in lesson m1-l5; it is worth noting here that it rounds out the set alongside `byteswap` and `unreachable` as the third low-level C++23 utility, replacing the verbose `static_cast<std::underlying_type_t<E>>(e)` with a concise call.",
      "examples": [
        {
          "id": "m9-l2-ex1",
          "title": "std::byteswap: byte-order conversion",
          "description": "Converting between host byte order and network byte order with `std::byteswap`. The function works in a `constexpr` context — a port number can be converted at compile time. `to_underlying` lets you print a numeric HTTP status code without a verbose `static_cast`.",
          "code": "#include <bit>\n#include <cstdint>\n#include <print>\n#include <utility>\n\nint main() {\n    // Network byte order conversion (big-endian)\n    uint32_t host_value = 0x12345678;\n    uint32_t network_value = std::byteswap(host_value);\n\n    std::println(\"host:    {:#010x}\", host_value);\n    std::println(\"network: {:#010x}\", network_value);\n    std::println(\"round-trip: {:#010x}\", std::byteswap(network_value));\n\n    // Works at compile time\n    constexpr uint16_t port = std::byteswap(uint16_t{8080});\n    std::println(\"port 8080 in network order: {:#06x}\", port);\n\n    // to_underlying\n    enum class Status { OK = 200, NotFound = 404, Error = 500 };\n    auto code = std::to_underlying(Status::NotFound);\n    std::println(\"HTTP code: {}\", code);\n}",
          "expectedOutput": "host:    0x12345678\nnetwork: 0x78563412\nround-trip: 0x12345678\nport 8080 in network order: 0x901f\nHTTP code: 404",
          "outputsVerified": true,
          "compiler": "gsnapshot",
          "compilerFlags": "-std=c++23 -O2"
        },
        {
          "id": "m9-l2-ex2",
          "title": "std::unreachable: exhaustive switch and the optimizer",
          "description": "`std::unreachable()` after the last `case` tells the optimizer that the default branch can never execute. This eliminates the compiler warning about \"not all paths returning a value\" and helps the code generator produce tighter output.",
          "code": "#include <utility>\n#include <print>\n#include <string_view>\n\nenum class Direction { North, South, East, West };\n\nstd::string_view direction_name(Direction d) {\n    switch (d) {\n        case Direction::North: return \"North\";\n        case Direction::South: return \"South\";\n        case Direction::East:  return \"East\";\n        case Direction::West:  return \"West\";\n    }\n    std::unreachable(); // all cases handled — helps optimizer skip default\n}\n\nint opposite_sign(int x) {\n    if (x > 0) return 1;\n    if (x < 0) return -1;\n    return 0;\n    // Note: no unreachable here — 0 is a valid path\n}\n\nint main() {\n    for (auto d : {Direction::North, Direction::South, Direction::East, Direction::West})\n        std::println(\"direction: {}\", direction_name(d));\n\n    std::println(\"sign(42): {}\", opposite_sign(42));\n    std::println(\"sign(-7): {}\", opposite_sign(-7));\n    std::println(\"sign(0): {}\", opposite_sign(0));\n}",
          "expectedOutput": "direction: North\ndirection: South\ndirection: East\ndirection: West\nsign(42): 1\nsign(-7): -1\nsign(0): 0",
          "outputsVerified": true,
          "compiler": "gsnapshot",
          "compilerFlags": "-std=c++23 -O2"
        }
      ],
      "outputsVerified": true
    },
    {
      "id": "m9-l3",
      "title": "`std::out_ptr`, `uz` Literals, `ranges::shift_left/right`: Final C++23 Utilities",
      "prerequisites": ["m9-l2"],
      "background": null,
      "motivation": "Three small but practically important additions in C++23 close long-standing friction points. The `uz` suffix finally gives `std::size_t` as the type of an integer literal — before this, `for (auto i = 0; i < vec.size(); ++i)` generated a `-Wsign-compare` warning with no clean way to suppress it. `std::out_ptr` and `std::inout_ptr` eliminate the awkward two-step pattern of \"first get a raw pointer from a C API, then wrap it in a `unique_ptr`\" — now it's a single call. `std::ranges::shift_left` and `std::ranges::shift_right` give a straightforward way to shift elements in a range in-place, replacing the indirect `std::rotate` trick or manual `std::move`. Each of the three features standardizes something programmers had been writing by hand or through non-standard extensions for years.",
      "theory": "The `uz` and `z` suffixes for integer literals (P0330) solve a classic problem: the type of the literal `0` is `int`, while `.size()` returns `std::size_t`. Comparing `int` with `size_t` is a signed/unsigned comparison and triggers the `-Wsign-compare` warning. Before C++23 the options were to explicitly write `size_t i = 0` or suppress the warning with a cast. Now the `uz` suffix produces the unsigned type `std::size_t` (unsigned size), while `z` produces the signed type `std::ptrdiff_t` (signed size). Both work with any numeric base: `0x10uz`, `0b1010z`. In practice `uz` is used more often — `.size()`, `.length()`, and `sizeof` all return `size_t`.\n\n`std::out_ptr` and `std::inout_ptr` (header `<memory>`, P1132) solve the problem of interoperating smart pointers with C APIs that take `T**` as an output parameter. The typical pattern before C++23: declare `T* raw = nullptr`, pass `&raw` to the C API, then manually call `.reset(raw)` or re-construct the `unique_ptr`. `std::out_ptr(ptr)` creates a temporary adaptor object that holds a `T*` and, when destroyed (at the end of the full expression), calls `ptr.reset(raw_ptr)` with whatever value the C API wrote. `std::inout_ptr` does the same but additionally extracts the current raw pointer from the smart pointer before passing it in — needed for C APIs that may reuse an already-allocated buffer. Both work with `unique_ptr`, `shared_ptr`, and any type that has a `.reset()` member.\n\n`std::ranges::shift_left` and `std::ranges::shift_right` (header `<algorithm>`, P1243) move elements of a range n positions to the left or right in-place. `shift_left(r, n)` moves each element n positions toward the beginning: `r[i] = move(r[i+n])`. It returns a subrange of the valid elements (the first `size - n` elements). `shift_right(r, n)` symmetrically shifts toward the end and returns the last `size - n` elements. Elements \"pushed off\" the edge remain in the range in a valid but unspecified state — they are not destroyed, but their values must not be relied upon. The range's size does not change. If `n >= size`, an empty subrange is returned. Practical uses include ring-buffer implementations, sliding-window algorithms, and efficient removal of elements from the front without `erase(begin())`, which would move the entire contents.\n\nAll three additions share one idea: eliminating specific friction that previously required non-standard extensions (`__builtin_bswap`, `__builtin_unreachable`), verbose patterns (the two-step `T* raw; api(&raw); ptr.reset(raw)`), or workarounds (`std::rotate` instead of a direct shift). C++23 systematically \"mops up\" such pain points — each small utility looks minor in isolation, but together they meaningfully reduce the cognitive overhead of real-world code.",
      "examples": [
        {
          "id": "m9-l3-ex1",
          "title": "`uz`/`z` literals and `ranges::shift_left/right`",
          "description": "The `uz` suffix eliminates the `-Wsign-compare` warning in an index-based loop. `shift_left` and `shift_right` shift elements in-place and return a subrange of the valid elements.",
          "code": "#include <vector>\n#include <algorithm>\n#include <ranges>\n#include <print>\n\nint main() {\n    // uz literals eliminate sign-comparison warnings\n    std::vector<int> v = {10, 20, 30, 40, 50};\n    for (auto i = 0uz; i < v.size(); ++i)\n        std::print(\"{} \", v[i]);\n    std::println(\"\");\n\n    // shift_left: shift elements left by 2\n    std::vector<int> data = {1, 2, 3, 4, 5};\n    auto valid = std::ranges::shift_left(data, 2);\n    std::print(\"after shift_left(2), valid part: \");\n    for (int x : valid) std::print(\"{} \", x);\n    std::println(\"\");\n    std::println(\"full vector size still: {}\", data.size());\n\n    // shift_right: shift elements right by 1\n    std::vector<int> data2 = {1, 2, 3, 4, 5};\n    auto valid2 = std::ranges::shift_right(data2, 1);\n    std::print(\"after shift_right(1), valid part: \");\n    for (int x : valid2) std::print(\"{} \", x);\n    std::println(\"\");\n}",
          "expectedOutput": "10 20 30 40 50 \nafter shift_left(2), valid part: 3 4 5 \nfull vector size still: 5\nafter shift_right(1), valid part: 1 2 3 4 ",
          "outputsVerified": true,
          "compiler": "gsnapshot",
          "compilerFlags": "-std=c++23 -O2"
        },
        {
          "id": "m9-l3-ex2",
          "title": "`std::out_ptr`: smart pointers and C APIs",
          "description": "`std::out_ptr` replaces the two-step pattern of \"get a raw pointer from a C API, then manually call `.reset()`\". The adaptor calls `.reset()` itself when it is destroyed at the end of the expression.",
          "code": "#include <memory>\n#include <print>\n\n// Simulate a C API that allocates a resource via an out-parameter\nstruct Resource {\n    int value;\n    explicit Resource(int v) : value(v) {}\n};\n\nint c_api_create(Resource** out, int val) {\n    *out = new Resource(val);\n    return 0; // success\n}\n\nvoid c_api_destroy(Resource* r) {\n    delete r;\n}\n\nint main() {\n    // Old pattern (before out_ptr):\n    {\n        Resource* raw = nullptr;\n        c_api_create(&raw, 42);\n        std::unique_ptr<Resource, decltype(&c_api_destroy)> ptr{raw, c_api_destroy};\n        std::println(\"old pattern value: {}\", ptr->value);\n    }\n\n    // New pattern with std::out_ptr:\n    {\n        std::unique_ptr<Resource, decltype(&c_api_destroy)> ptr{nullptr, c_api_destroy};\n        c_api_create(std::out_ptr(ptr), 99);\n        std::println(\"out_ptr pattern value: {}\", ptr->value);\n    }\n\n    std::println(\"both resources properly destroyed\");\n}",
          "expectedOutput": "old pattern value: 42\nout_ptr pattern value: 99\nboth resources properly destroyed",
          "outputsVerified": true,
          "compiler": "gsnapshot",
          "compilerFlags": "-std=c++23 -O2"
        }
      ],
      "outputsVerified": true
    }
  ]
}
