{
  "id": "m1",
  "moduleNumber": 1,
  "title": "Language improvements",
  "significance": "basic",
  "prerequisites": [
    "m0"
  ],
  "lessons": [
    {
      "id": "m1-l1",
      "title": "if consteval and auto Function Parameters",
      "prerequisites": [],
      "background": {
        "title": "consteval in C++20",
        "body": "consteval (C++20) declares a function as \"immediate\": it can only be called in a constant evaluation context. Unlike constexpr, which can run both at compile time and at runtime, consteval always executes exclusively at compile time."
      },
      "motivation": "A constexpr function can be called both at compile time and at runtime. This flexibility is useful, but it creates a problem: if you want to call a consteval function (which is compile-time only) from inside a constexpr function, the compiler rejects it — it cannot know whether execution is happening at compile time. Before C++23, the only workaround was std::is_constant_evaluated() from C++20, but it had subtle pitfalls. Separately, auto in ordinary function parameters was only valid in lambdas. C++23 solves both issues: if consteval provides a reliable way to branch on the evaluation context, and auto in ordinary function parameters turns them into abbreviated function templates.",
      "theory": "if consteval is a new branching construct introduced in C++23. Unlike if constexpr, it takes no condition expression: instead, it checks whether the current evaluation is happening in a constant evaluation context. The true branch executes only at compile time; the false branch executes only at runtime. The critical property: the true branch is itself a \"manifestly constant-evaluated context\", so consteval functions may be called inside it — exactly what was impossible with std::is_constant_evaluated(). A negated form is also available: if !consteval { ... } executes only at runtime.\n\nThe second C++23 feature is auto in ordinary function parameters. Since C++14, lambdas have supported auto parameters to create generic lambdas. C++23 extends this syntax to regular (named) functions. Writing void print(auto x) is equivalent to template<typename T> void print(T x): each auto parameter becomes a separate template parameter. This is called an \"abbreviated function template\".\n\nauto parameters fully support qualifiers: const auto&, auto&&, auto*, and so on. Multiple auto parameters each introduce an independent template parameter — so void combine(auto a, auto b) can accept an int and a double simultaneously. During overload resolution, a function with a concrete type takes priority over a generic one: if both void describe(const std::string&) and void describe(const auto&) exist, the former is chosen for a std::string argument.\n\nBoth features make C++23 code more expressive without sacrificing performance. if consteval eliminates an entire class of errors that arose when consteval functions were called in the wrong context. auto parameters reduce template boilerplate: there is no need to write template<typename T> for straightforward generic functions.",
      "examples": [
        {
          "id": "m1-l1-ex1",
          "title": "if consteval: different paths at compile time vs runtime",
          "code": "#include <print>\n#include <cmath>\n\nconsteval double compile_time_sqrt(double x) {\n    // Newton's method — evaluated only at compile time\n    double r = x;\n    for (int i = 0; i < 10; ++i) r = (r + x/r) / 2;\n    return r;\n}\n\nconstexpr double my_sqrt(double x) {\n    if consteval {\n        return compile_time_sqrt(x); // safe: we are in a compile-time context\n    } else {\n        return std::sqrt(x); // runtime: use libm\n    }\n}\n\nint main() {\n    constexpr double ct = my_sqrt(2.0); // compile-time path\n    double rt = my_sqrt(2.0);           // runtime path\n    std::println(\"compile-time: {:.6f}\", ct);\n    std::println(\"runtime:      {:.6f}\", rt);\n}",
          "expectedOutput": "compile-time: 1.414214\nruntime:      1.414214",
          "outputsVerified": true,
          "explanation": "my_sqrt is declared constexpr, so it can be called both at compile time and at runtime. Inside the if consteval branch the call to compile_time_sqrt is valid — we are guaranteed to be in a constant evaluation context. Without if consteval the compiler would reject the consteval call because it cannot know the context. Both paths produce the same result but use different implementations."
        },
        {
          "id": "m1-l1-ex2",
          "title": "auto function parameters: generic functions and overloading",
          "code": "#include <print>\n#include <string>\n\n// abbreviated function template — instantiated for each distinct argument type\nvoid describe(const auto& x) {\n    std::println(\"value: {}\", x);\n}\n\n// overload for string — more specific, wins for string arguments\nvoid describe(const std::string& s) {\n    std::println(\"string of length {}: {}\", s.size(), s);\n}\n\n// multiple auto params — each is a separate template parameter\nauto add(auto a, auto b) {\n    return a + b;\n}\n\nint main() {\n    describe(42);\n    describe(3.14);\n    describe(std::string{\"hello\"});\n\n    std::println(\"add: {}\", add(1, 2));\n    std::println(\"add: {}\", add(1.5, 2.5));\n    std::println(\"add: {}\", add(std::string{\"foo\"}, std::string{\"bar\"}));\n}",
          "expectedOutput": "value: 42\nvalue: 3.14\nstring of length 5: hello\nadd: 3\nadd: 4\nadd: foobar",
          "outputsVerified": true,
          "explanation": "describe(const auto& x) is an abbreviated function template, equivalent to template<typename T> void describe(const T& x). When called with std::string{\"hello\"}, the compiler selects the more specialized overload describe(const std::string&). add(auto a, auto b) introduces two independent template parameters: a and b may have different types. For strings, operator+ performs concatenation."
        }
      ],
      "outputsVerified": true
    },
    {
      "id": "m1-l2",
      "title": "Multidimensional operator[]: m[i, j] Syntax",
      "prerequisites": ["m1-l1"],
      "background": null,
      "motivation": "Before C++23, operator[] could only take a single argument. Writing m[i, j] inside the brackets was interpreted by the compiler as the comma operator: m[i, j] meant m[(i, j)], which evaluates to m[j] — the right operand. C++20 deprecated this behaviour, and C++23 made it ill-formed. To index two-dimensional structures, developers resorted to workarounds: operator() in the style of m(i, j), chained subscripts m[i][j] via row proxy objects, or a helper index type m[{i, j}]. None of these feel natural for indexing. C++23 removed the restriction: operator[] can now accept any number of parameters, and commas inside [] separate arguments rather than invoking the comma operator.",
      "theory": "C++23 allows operator[] to accept an arbitrary number of parameters — zero, one, two, or more. The declaration syntax is identical to any other operator overload: parameters are listed separated by commas. Commas inside [] at the call site now separate arguments rather than invoking the comma operator. The old behaviour (comma operator in subscript context) was deprecated in C++20 and became ill-formed in C++23, so legacy code that accidentally wrote m[i, j] to mean m[j] will no longer compile.\n\nParameters of operator[] can have any types and qualifiers. Both a mutating and a non-mutating overload can coexist: double& operator[](size_t i, size_t j) for writes and const double& operator[](size_t i, size_t j) const for reads. A variadic form using auto... is also valid, but for concrete data structures a fixed number of parameters with the appropriate types is more common.\n\nThe primary motivation for this language change was std::mdspan — a multidimensional array view added to the standard library in C++23. mdspan stores a pointer to a one-dimensional buffer and a mapping from multi-index to flat offset. The expression md[i, j] invokes a two-argument operator[] that computes the offset internally. Before this language change it was impossible to give mdspan an intuitive subscript interface, which is why the language feature and the library component landed together in C++23.\n\nAn operator with zero arguments — operator[]() — is technically valid since C++23, though rarely needed: it can be useful in proxy objects or DSL builders where a subscript without an index carries semantic meaning. One important note: the change only affects the subscript operator itself. Calling a[i, j] where a is a plain built-in array or a type that does not define a matching operator[] remains an error.",
      "examples": [
        {
          "id": "m1-l2-ex1",
          "title": "Simple 3x3 matrix with operator[](size_t, size_t)",
          "code": "#include <print>\n#include <cstddef>\n\nstruct Matrix3x3 {\n    double data[3][3]{};\n\n    double& operator[](size_t i, size_t j) { return data[i][j]; }\n    const double& operator[](size_t i, size_t j) const { return data[i][j]; }\n};\n\nint main() {\n    Matrix3x3 m;\n    m[0, 0] = 1.0;\n    m[1, 1] = 2.0;\n    m[2, 2] = 3.0;\n\n    for (size_t i = 0; i < 3; ++i) {\n        for (size_t j = 0; j < 3; ++j) {\n            std::print(\"{:.0f} \", m[i, j]);\n        }\n        std::println(\"\");\n    }\n}",
          "expectedOutput": "1 0 0 \n0 2 0 \n0 0 3 ",
          "outputsVerified": true,
          "explanation": "Both overloads of operator[] take two size_t parameters. The expression m[0, 0] = 1.0 calls the non-const overload (returning double&), and m[i, j] in the loop body calls the const overload. Before C++23 this would not compile: the compiler would treat m[0, 0] as the comma operator and invoke a single-parameter operator[] with argument 0."
        },
        {
          "id": "m1-l2-ex2",
          "title": "3D tensor with operator[](size_t, size_t, size_t)",
          "code": "#include <print>\n#include <cstddef>\n\nstruct Tensor3D {\n    double data[2][2][2]{};\n\n    double& operator[](size_t i, size_t j, size_t k) {\n        return data[i][j][k];\n    }\n};\n\nint main() {\n    Tensor3D t;\n    t[0, 0, 0] = 1.0;\n    t[0, 0, 1] = 2.0;\n    t[1, 0, 0] = 3.0;\n    t[1, 1, 1] = 4.0;\n\n    std::println(\"t[0,0,0] = {:.0f}\", t[0, 0, 0]);\n    std::println(\"t[0,0,1] = {:.0f}\", t[0, 0, 1]);\n    std::println(\"t[1,0,0] = {:.0f}\", t[1, 0, 0]);\n    std::println(\"t[1,1,1] = {:.0f}\", t[1, 1, 1]);\n}",
          "expectedOutput": "t[0,0,0] = 1\nt[0,0,1] = 2\nt[1,0,0] = 3\nt[1,1,1] = 4",
          "outputsVerified": true,
          "explanation": "operator[] can now take three arguments. The syntax t[0, 0, 0] reads as naturally as indexing a built-in three-dimensional array. Internally the operator simply delegates to data[i][j][k] — no proxy objects required."
        },
        {
          "id": "m1-l2-ex3",
          "title": "Dynamic grid with bounds checking — the same pattern as std::mdspan",
          "code": "#include <print>\n#include <vector>\n#include <stdexcept>\n#include <cstddef>\n\n// Dynamic 2D grid — the same pattern that std::mdspan uses internally:\n// a flat storage buffer + flat-index computation inside operator[]\nstruct Grid {\n    size_t rows, cols;\n    std::vector<double> data;\n\n    Grid(size_t r, size_t c) : rows(r), cols(c), data(r * c, 0.0) {}\n\n    double& operator[](size_t i, size_t j) {\n        if (i >= rows || j >= cols) throw std::out_of_range(\"Grid: index out of range\");\n        return data[i * cols + j];\n    }\n\n    double operator[](size_t i, size_t j) const {\n        if (i >= rows || j >= cols) throw std::out_of_range(\"Grid: index out of range\");\n        return data[i * cols + j];\n    }\n};\n\nint main() {\n    Grid g{3, 4};\n    g[0, 0] = 1.0;\n    g[1, 2] = 5.0;\n    g[2, 3] = 9.0;\n\n    std::println(\"g[0,0] = {:.0f}\", g[0, 0]);\n    std::println(\"g[1,2] = {:.0f}\", g[1, 2]);\n    std::println(\"g[2,3] = {:.0f}\", g[2, 3]);\n    std::println(\"g[0,1] = {:.0f}\", g[0, 1]);\n\n    try {\n        [[maybe_unused]] double x = g[5, 0]; // out of bounds\n    } catch (const std::out_of_range& e) {\n        std::println(\"caught: {}\", e.what());\n    }\n}",
          "expectedOutput": "g[0,0] = 1\ng[1,2] = 5\ng[2,3] = 9\ng[0,1] = 0\ncaught: Grid: index out of range",
          "outputsVerified": true,
          "explanation": "Grid stores its data in a flat std::vector and computes the offset i * cols + j inside operator[] — exactly what std::mdspan does under the hood. The non-const overload returns double& for writes; the const overload returns by value for reads. The bounds check shows that a two-parameter operator[] is not restricted in any way: it can contain arbitrary logic."
        }
      ],
      "outputsVerified": true
    },
    {
      "id": "m1-l3",
      "title": "Language improvements - Lesson 3",
      "stub": true
    },
    {
      "id": "m1-l4",
      "title": "Language improvements - Lesson 4",
      "stub": true
    },
    {
      "id": "m1-l5",
      "title": "Language improvements - Lesson 5",
      "stub": true
    }
  ]
}
