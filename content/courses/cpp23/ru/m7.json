{
  "id": "m7",
  "moduleNumber": 7,
  "title": "Контейнеры и утилиты",
  "significance": "important",
  "prerequisites": [
    "m6"
  ],
  "lessons": [
    {
      "id": "m7-l1",
      "title": "std::flat_map и std::flat_set: ассоциативные контейнеры над массивом",
      "prerequisites": [],
      "background": null,
      "motivation": "std::map и std::set — рабочие лошадки C++, но под капотом у них красно-чёрное дерево: каждый узел — отдельная аллокация в куче, переход к следующему элементу — разыменование указателя. На read-heavy нагрузках (поиск, итерация) это означает постоянные cache miss'ы. C++23 добавил std::flat_map и std::flat_set — контейнеры с тем же интерфейсом, но хранящие данные в отсортированных векторах. Бинарный поиск по вектору на порядок кэш-дружелюбнее дерева, и для типичного use case «заполнить один раз, читать много раз» это даёт ощутимый выигрыш без изменения кода.",
      "theory": "std::flat_map<K, V> хранит ключи и значения в двух отдельных отсортированных векторах. Поиск выполняется бинарным поиском по вектору ключей за O(log n), как и в std::map, но обращение к последовательным ключам идёт по непрерывной памяти — процессорный кэш используется эффективно. Заголовок: <flat_map>. std::flat_set<T> аналогично хранит элементы в одном отсортированном векторе; заголовок: <flat_set>.\n\nИнтерфейс flat_map и flat_set намеренно совпадает с map/set: operator[], at(), find(), contains(), insert(), erase(), итерация по парам [key, value]. Переход с map на flat_map часто сводится к замене одного имени типа. Важное отличие касается итераторов: разыменование итератора flat_map возвращает proxy-пару ссылок (std::pair<const K&, V&>), которая является временным объектом — структурированные привязки нужно объявлять через auto [k, v], а не через auto& [k, v].\n\nПроизводительность зависит от паттерна доступа. flat_map выигрывает при чтении, итерации и небольших/средних наборах данных (тысячи-десятки тысяч элементов). Проигрывает при частых вставках в середину: каждая вставка требует сдвига элементов за O(n). Если данные уже отсортированы, тег std::sorted_unique позволяет сконструировать контейнер без лишней сортировки. Правило выбора: строите таблицу один раз и потом только читаете — flat_map; активно вставляете/удаляете в произвольных местах — обычный map.\n\nflat_map открывает доступ к базовым контейнерам через методы keys() и values(), возвращающие константные ссылки на внутренние векторы. Это позволяет передать список ключей в алгоритм без копирования, использовать SIMD-дружелюбный доступ или просто получить размер без прохода по всему контейнеру. По умолчанию базовый контейнер — std::vector, но через параметры шаблона можно подставить std::deque или любой другой контейнер с произвольным доступом.",
      "examples": [
        {
          "id": "m7-l1-ex1",
          "title": "Базовый flat_map: тот же интерфейс, что у map",
          "level": "basic",
          "code": "#include <flat_map>\n#include <print>\n#include <string>\nint main() {\n    std::flat_map<std::string, int> scores;\n    scores[\"Alice\"] = 95;\n    scores[\"Bob\"] = 87;\n    scores[\"Carol\"] = 92;\n    scores[\"Alice\"] = 98;\n    for (auto [name, score] : scores)\n        std::println(\"{}: {}\", name, score);\n    std::println(\"contains Dave: {}\", scores.contains(\"Dave\"));\n}",
          "expectedOutput": "Alice: 98\nBob: 87\nCarol: 92\ncontains Dave: false",
          "explanation": "flat_map ведёт себя как map: дублирующая запись scores[\"Alice\"] = 98 обновляет значение, итерация идёт в отсортированном по ключу порядке, contains() работает через бинарный поиск. Обратите внимание на auto [name, score] без &: итератор flat_map возвращает proxy-пару временных ссылок, привязка по lvalue-ссылке не компилируется."
        },
        {
          "id": "m7-l1-ex2",
          "title": "flat_set и тег sorted_unique",
          "level": "intermediate",
          "code": "#include <flat_map>\n#include <flat_set>\n#include <print>\nint main() {\n    std::flat_set<int> nums;\n    nums.insert(7);\n    nums.insert(2);\n    nums.insert(5);\n    nums.insert(3);\n    nums.insert(2); // дубликат игнорируется\n    std::print(\"sorted:\");\n    for (int x : nums)\n        std::print(\" {}\", x);\n    std::println(\"\");\n\n    std::flat_map<int, std::string> m(\n        std::sorted_unique,\n        {{1, \"one\"}, {2, \"two\"}, {3, \"three\"}}\n    );\n    for (auto [k, v] : m)\n        std::println(\"{} -> {}\", k, v);\n}",
          "expectedOutput": "sorted: 2 3 5 7\n1 -> one\n2 -> two\n3 -> three",
          "explanation": "flat_set хранит элементы в отсортированном векторе и автоматически отбрасывает дубликаты при вставке. Тег std::sorted_unique при конструировании flat_map сигнализирует, что входные данные уже упорядочены и уникальны — контейнер пропускает O(n log n) сортировку и принимает данные напрямую. Используйте этот тег только тогда, когда гарантируете порядок и уникальность: нарушение приводит к UB."
        },
        {
          "id": "m7-l1-ex3",
          "title": "Таблица конфигурации: find() и keys()",
          "level": "advanced",
          "code": "#include <flat_map>\n#include <print>\n#include <string>\n#include <vector>\nint main() {\n    std::flat_map<std::string, int> config = {\n        {\"buffer_size\", 4096}, {\"max_conn\", 100},\n        {\"port\", 8080}, {\"retries\", 3}, {\"timeout\", 30}\n    };\n    for (const auto& key : {\"port\", \"retries\", \"missing\"}) {\n        auto it = config.find(key);\n        if (it != config.end())\n            std::println(\"{} = {}\", key, it->second);\n        else\n            std::println(\"{} not found\", key);\n    }\n    // keys() возвращает ссылку на внутренний вектор ключей\n    std::println(\"key count: {}\", config.keys().size());\n}",
          "expectedOutput": "port = 8080\nretries = 3\nmissing not found\nkey count: 5",
          "explanation": "flat_map идеально подходит для таблиц конфигурации: строится один раз при старте программы, затем используется только для чтения. find() выполняет бинарный поиск и возвращает итератор или end(). Метод keys() открывает прямой доступ к внутреннему вектору ключей без копирования — удобно для передачи в алгоритмы или логирования. Аналогично values() даёт доступ к вектору значений."
        }
      ],
      "outputsVerified": true
    },
    {
      "id": "m7-l2",
      "title": "std::move_only_function и std::spanstream",
      "prerequisites": ["m7-l1"],
      "background": null,
      "motivation": "std::function<Sig> — удобная обёртка для хранения любого вызываемого объекта, но у неё есть скрытое ограничение: хранимый callable обязан быть копируемым. Это блокирует захват unique_ptr, std::promise или любого другого move-only ресурса в лямбду, которую нужно положить в std::function. C++23 решает проблему через std::move_only_function<Sig> — она снимает требование копируемости и принимает любой перемещаемый callable. Второй утилитой в этом уроке является std::spanstream: std::stringstream выделяет буфер в куче при каждом создании, что неприемлемо в embedded-коде с запретом на аллокации или в real-time-обработчиках. std::ospanstream и std::ispanstream работают поверх фиксированного внешнего буфера без единого обращения к куче.",
      "theory": "std::move_only_function<Sig> объявлена в заголовке <functional> и ведёт себя как std::function, но является move-only типом: её можно переместить, но не скопировать. Это позволяет хранить лямбды, захватывающие std::unique_ptr, std::promise, файловые дескрипторы или любой другой ресурс, для которого копирование не определено. Синтаксис вызова остаётся прежним: f(args...). Два метода, специфичных для std::function — .target() и .target_type() — в move_only_function отсутствуют, что несколько упрощает реализацию и даёт чуть лучшую производительность.\n\nПрактические сценарии: очереди задач (каждая задача владеет уникальным ресурсом), цепочки обратных вызовов (promise/future), конвейеры обработки, где каждый шаг несёт эксклюзивное владение данными. Попытка скопировать move_only_function вызывает ошибку компиляции — это явный, статически проверяемый контракт вместо молчаливого UB или runtime-исключения.\n\nstd::spanstream — семейство потоков ввода-вывода из заголовка <spanstream>, работающих поверх внешней памяти. std::ospanstream принимает std::span<char> и записывает данные в этот буфер операторами <<, не выделяя кучную память. После записи метод .span() возвращает span, ограниченный фактически записанными байтами. std::ispanstream принимает std::span<const char> и разбирает его операторами >> в точности как std::istringstream. Оба класса унаследованы от std::basic_spanstream, который при необходимости поддерживает и чтение, и запись.\n\nГлавная ценность spanstream — предсказуемое использование памяти: буфер живёт ровно столько, сколько нужно вызывающей стороне, и никакого скрытого heap-трафика нет. Это важно в системах реального времени, embedded-платформах с ограниченной кучей или в высоконагруженных серверах, где аллокатор становится точкой конкуренции. Ограничение: ospanstream не умеет расти — если данных больше, чем размер буфера, запись молча усекается и поток переходит в состояние ошибки, которое нужно проверять через os.good() или os.tellp().",
      "examples": [
        {
          "id": "m7-l2-ex1",
          "title": "move_only_function: хранение лямбды с unique_ptr",
          "level": "basic",
          "code": "#include <functional>\n#include <memory>\n#include <print>\n#include <vector>\nint main() {\n    std::vector<std::move_only_function<void()>> tasks;\n    for (int i = 0; i < 3; ++i) {\n        auto data = std::make_unique<int>(i * 10);\n        tasks.push_back([p = std::move(data)]() {\n            std::println(\"value: {}\", *p);\n        });\n    }\n    for (auto& t : tasks) t();\n}",
          "expectedOutput": "value: 0\nvalue: 10\nvalue: 20",
          "explanation": "Лямбда захватывает unique_ptr через std::move — она сама становится move-only. std::function отказала бы при попытке скопировать такой callable в вектор. std::move_only_function принимает перемещение без возражений: каждая задача в векторе владеет своим ресурсом эксклюзивно."
        },
        {
          "id": "m7-l2-ex2",
          "title": "move_only_function как параметр функции и перемещение владения",
          "level": "intermediate",
          "code": "#include <functional>\n#include <memory>\n#include <print>\n#include <string>\nvoid run(std::move_only_function<std::string()> producer) {\n    std::println(\"produced: {}\", producer());\n}\nint main() {\n    auto counter = std::make_unique<int>(0);\n    run([c = std::move(counter)]() mutable -> std::string {\n        return \"count=\" + std::to_string(++(*c));\n    });\n    // move_only_function can be moved\n    auto f = std::move_only_function<int(int)>([](int x){ return x * 2; });\n    auto g = std::move(f);\n    std::println(\"g(21) = {}\", g(21));\n}",
          "expectedOutput": "produced: count=1\ng(21) = 42",
          "explanation": "Параметр run() принимает move_only_function по значению — это передача владения callable'ом вместе со всеми его захваченными ресурсами. Второй блок демонстрирует явное перемещение: после std::move(f) переменная f находится в valid-but-unspecified состоянии, а g полностью владеет хранимой функцией. Попытка скопировать f вместо перемещения дала бы ошибку компиляции."
        },
        {
          "id": "m7-l2-ex3",
          "title": "ospanstream и ispanstream: потоковый I/O без выделения памяти",
          "level": "advanced",
          "code": "#include <spanstream>\n#include <print>\n#include <span>\n#include <string_view>\nint main() {\n    // ospanstream: write into stack buffer\n    char buf[64]{};\n    {\n        std::ospanstream os(std::span<char>{buf, sizeof(buf)});\n        os << \"x=\" << 42 << \" y=\" << 3.14;\n        auto written = os.span();\n        std::println(\"written: {}\", std::string_view(written.data(), written.size()));\n    }\n    // ispanstream: parse from existing buffer\n    const char input[] = \"100 200 300\";\n    std::ispanstream is(std::span<const char>{input, sizeof(input) - 1});\n    int a, b, c;\n    is >> a >> b >> c;\n    std::println(\"parsed: {} {} {}\", a, b, c);\n}",
          "expectedOutput": "written: x=42 y=3.14\nparsed: 100 200 300",
          "explanation": "ospanstream записывает текстовое представление значений прямо в стековый массив buf. После записи os.span() возвращает span только по фактически записанным байтам — не весь буфер целиком. ispanstream разбирает буфер ровно так, как istringstream разбирал бы строку, но без аллокации. Обратите внимание на явный std::span<const char>: конструктор испанстрима принимает span, а не указатель, и неявного преобразования из braced-init нет."
        }
      ],
      "outputsVerified": true
    },
    {
      "id": "m7-l3",
      "title": "Контейнеры и утилиты - Lesson 3",
      "stub": true
    },
    {
      "id": "m7-l4",
      "title": "Контейнеры и утилиты - Lesson 4",
      "stub": true
    }
  ]
}
