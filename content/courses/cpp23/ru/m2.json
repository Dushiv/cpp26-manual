{
  "id": "m2",
  "moduleNumber": 2,
  "title": "Deducing this",
  "significance": "flagship",
  "prerequisites": [
    "m1"
  ],
  "lessons": [
    {
      "id": "m2-l1",
      "title": "Explicit Object Parameter: синтаксис и базовые паттерны",
      "prerequisites": [],
      "background": null,
      "motivation": "В C++17 написать метод-геттер, корректно работающий для всех комбинаций константности и категорий значений объекта, означало дублировать тело метода в двух, а то и четырёх перегрузках. Для простого поля нужны T& get() &, const T& get() const&, T&& get() && и const T&& get() const&&. Все четыре содержат одинаковую логику — отличаются лишь квалификаторы. Любое изменение логики надо вносить сразу в несколько мест, что ведёт к ошибкам и раздуванию кода. C++23 решает эту проблему через явный параметр объекта (explicit object parameter, «deducing this»): квалификатор объекта превращается в обычный параметр шаблона, и компилятор выводит его тип автоматически при каждом вызове.",
      "theory": "Явный параметр объекта объявляется как первый параметр нестатического метода с ключевым словом this перед именем: auto& get(this auto& self). Такой параметр принято называть self, хотя имя произвольное. Тип self выводится компилятором по правилам вывода шаблонных аргументов в зависимости от того, на каком объекте вызван метод: если объект — lvalue типа Widget, Self = Widget; если const lvalue — Self = const Widget; если rvalue — Self = Widget (без амперсанда, но ссылка на self будет rvalue-ссылкой). Компилятор подставляет конкретный тип при каждом вызове, как при вызове шаблонной функции.\n\nСинтаксис вызова не меняется: w.get() работает так же, как и раньше. Компилятор сам передаёт объект в параметр self. Явный параметр объекта нельзя сочетать с virtual и static — метод должен быть обычным нестатическим.\n\nГлавный практический эффект — устранение дублирования const/non-const перегрузок. Вместо четырёх вариантов get() достаточно одного: auto&& get(this auto&& self). Здесь self выводится в нужную ссылочную категорию автоматически. Чтобы правильно вернуть член с сохранением категории значения объекта, используется std::forward_like<decltype(self)>(self.member): эта утилита C++23 применяет к члену те же константность и ссылочность, что и у self.\n\nДля цепочных вызовов (fluent interface, builder-паттерн) явный параметр объекта также удобен: auto& set_x(this auto& self, X val) возвращает self, и на возвращённой ссылке можно сразу вызвать следующий метод. Тип возврата автоматически подстраивается под фактический тип объекта, что важно при наследовании — производный класс получает ссылку на себя, а не на базовый.\n\nКатегории значений self: когда параметр объявлен как this auto& self, self всегда lvalue-ссылка, и передача rvalue-объектов запрещена. Если нужно обрабатывать rvalue, используют this auto&& self — универсальная ссылка, которая связывается с любой категорией. Внутри метода для идеального проброса самого self используют std::forward<Self>(self), где Self — выведенный тип.\n\nОграничения: метод с явным параметром объекта нельзя объявить виртуальным — виртуальная диспетчеризация несовместима с шаблонным выводом типа. Также нельзя применять квалификаторы &, && или const к самому методу — все они теперь выражены через тип self.",
      "examples": [
        {
          "id": "m2-l1-ex1",
          "title": "Базовый синтаксис: один метод вместо двух перегрузок",
          "level": "basic",
          "code": "#include <iostream>\n#include <string>\n\nstruct Box {\n    std::string value;\n    auto& get(this auto& self) { return self.value; }\n};\n\nint main() {\n    Box b{\"hello\"};\n    const Box cb{\"world\"};\n    b.get() = \"hi\";\n    std::cout << b.get() << \"\\n\";\n    std::cout << cb.get() << \"\\n\";\n}",
          "expectedOutput": "hi\nworld",
          "explanation": "Метод get принимает self по ссылке на auto. Для b (Widget&) Self выводится как Box, и метод возвращает std::string&, что позволяет присвоить значение. Для cb (const Box&) Self выводится как const Box, и метод возвращает const std::string& — запись через него была бы ошибкой компиляции. Один метод покрывает оба случая без дублирования тела."
        },
        {
          "id": "m2-l1-ex2",
          "title": "Устранение четырёх перегрузок: std::forward_like",
          "level": "intermediate",
          "code": "#include <iostream>\n#include <utility>\n\nstruct Buffer {\n    int data = 42;\n    // C++23: один метод обрабатывает все категории значений\n    auto&& get(this auto&& self) {\n        return std::forward_like<decltype(self)>(self.data);\n    }\n};\n\nint main() {\n    Buffer b;\n    const Buffer cb;\n    std::cout << b.get() << \"\\n\";        // lvalue\n    std::cout << cb.get() << \"\\n\";       // const lvalue\n    std::cout << Buffer{}.get() << \"\\n\"; // rvalue\n}",
          "expectedOutput": "42\n42\n42",
          "explanation": "Параметр this auto&& self — универсальная ссылка, выводится в int& для b, const int& для cb и int&& для временного Buffer{}. std::forward_like<decltype(self)>(self.data) переносит на член data те же константность и категорию значения, что у self: для rvalue-объекта data возвращается как int&&, что позволяет переместить его, а не копировать. В C++17 этот эффект достигался четырьмя отдельными перегрузками."
        },
        {
          "id": "m2-l1-ex3",
          "title": "Fluent builder: цепочка методов через явный параметр объекта",
          "level": "advanced",
          "code": "#include <iostream>\n#include <string>\n\nstruct Config {\n    std::string host = \"localhost\";\n    int port = 8080;\n\n    auto& set_host(this auto& self, std::string h) {\n        self.host = std::move(h);\n        return self;\n    }\n    auto& set_port(this auto& self, int p) {\n        self.port = p;\n        return self;\n    }\n    void print() const {\n        std::cout << host << \":\" << port << \"\\n\";\n    }\n};\n\nint main() {\n    Config c;\n    c.set_host(\"example.com\").set_port(443).print();\n}",
          "expectedOutput": "example.com:443",
          "explanation": "Методы set_host и set_port принимают self по lvalue-ссылке и возвращают её же. Тип возврата auto& выводится как Config& — ровно тот тип, что нужен для продолжения цепочки. При наследовании этот же приём автоматически возвращает ссылку на производный класс, а не на базовый, что в C++17 требовало CRTP или отдельных реализаций в каждом наследнике."
        }
      ],
      "outputsVerified": true
    },
    {
      "id": "m2-l2",
      "title": "Рекурсивные лямбды и замена CRTP",
      "prerequisites": ["m2-l1"],
      "background": {
        "title": "CRTP в C++17",
        "body": "CRTP (Curiously Recurring Template Pattern) — идиома C++17, при которой базовый класс параметризуется производным: template<typename Derived> struct Base { ... }; struct Concrete : Base<Concrete> { ... };. Это позволяет Base вызывать методы Concrete через static_cast<Derived*>(this) без накладных расходов виртуальных функций — статический полиморфизм и примешивание (mixin). Недостатки: громоздкий шаблонный параметр на каждом базовом классе, небезопасный downcast, который становится UB при неправильном наследовании, и неочевидный синтаксис, осложняющий чтение кода."
      },
      "motivation": "До C++23 рекурсивная лямбда требовала либо обёртки std::function с накладными расходами на выделение памяти и стирание типов, либо трюка с Y-комбинатором, который сложно читать. Паттерн CRTP для статического полиморфизма и примесей также страдал от многословного шаблонного параметра и небезопасного downcast. Явный параметр объекта (deducing this) решает обе задачи: лямбда может ссылаться на себя через параметр self без std::function, а базовый класс может вызывать методы производного без шаблонного параметра и без cast.",
      "theory": "Рекурсивные лямбды до C++23 писались так: лямбда захватывала себя через std::function<int(int)> wrapper, что влекло выделение памяти в куче и стирание типа. Альтернативой был Y-комбинатор — функция высшего порядка, которая передаёт лямбду саму себе, — но её синтаксис труден для восприятия. С явным параметром объекта лямбда получает собственный тип замыкания как шаблонный параметр self, и может вызывать себя напрямую: auto fib = [](this auto self, int n) -> int { return n <= 1 ? n : self(n-1) + self(n-2); };. Параметр this auto self означает: «выведи тип замыкания и передай его как self по значению». Накладных расходов нет — компилятор инстанцирует конкретную функцию, как любой шаблон. Явная аннотация возвращаемого типа (-> int) здесь обязательна, поскольку компилятор не может вывести рекурсивный тип автоматически.\n\nЗамена CRTP работает иначе. В классическом CRTP базовый класс знает тип производного через шаблонный параметр и обращается к нему через static_cast<Derived*>(this). Это небезопасно: при неправильной иерархии cast даст UB. С deducing this базовый класс вообще не нуждается в шаблонном параметре: void interface(this auto& self) { self.implementation(); }. При вызове derived.interface() компилятор выводит Self = Derived и передаёт объект как self — вызов self.implementation() разрешается на фактическом типе без какого-либо cast. Если производный класс не определяет implementation(), получаем понятную ошибку компиляции. Если определяет — вызывается нужная функция.\n\nПаттерн Overloaded для std::visit — ещё одно применение вариадических базовых классов. Шаблон template<typename... Ts> struct Overloaded : Ts... { using Ts::operator()...; }; агрегирует несколько лямбд и открывает все их operator() в одном типе. std::visit передаёт хранящееся в variant значение в подходящий operator() по типу. Это чище, чем писать if-else по std::holds_alternative, и безопаснее, чем switch по индексу: компилятор проверяет, что обработаны все варианты.\n\nВсе три идиомы объединяет одна механика: компилятор выводит точный тип на месте вызова и инстанцирует конкретную функцию, избавляя разработчика от явного cast, шаблонных параметров и вспомогательных обёрток. Результирующий код короче, читается как обычный вызов метода, и полностью проверяется системой типов.",
      "examples": [
        {
          "id": "m2-l2-ex1",
          "title": "Рекурсивная лямбда: числа Фибоначчи без std::function",
          "level": "basic",
          "code": "#include <iostream>\n\nint main() {\n    auto fib = [](this auto self, int n) -> int {\n        if (n <= 1) return n;\n        return self(n - 1) + self(n - 2);\n    };\n    std::cout << fib(10) << \"\\n\";\n}",
          "expectedOutput": "55",
          "explanation": "Параметр this auto self означает, что тип замыкания самой лямбды выводится и передаётся в self по значению. Внутри тела self(n-1) вызывает ту же лямбду рекурсивно. Явный тип возврата -> int обязателен: без него компилятор не может вывести тип из рекурсивного тела. По сравнению со std::function нет выделения памяти в куче и нет стирания типа — компилятор генерирует обычную шаблонную функцию."
        },
        {
          "id": "m2-l2-ex2",
          "title": "Замена CRTP: примесь Printable без шаблонного параметра",
          "level": "intermediate",
          "code": "#include <iostream>\n#include <string>\n\nstruct Printable {\n    void print_info(this auto& self) {\n        std::cout << \"[Info] \" << self.describe() << \"\\n\";\n    }\n};\n\nstruct Cat : Printable {\n    std::string name;\n    explicit Cat(std::string n) : name(std::move(n)) {}\n    std::string describe() const { return \"Cat: \" + name; }\n};\n\nstruct Dog : Printable {\n    std::string name;\n    explicit Dog(std::string n) : name(std::move(n)) {}\n    std::string describe() const { return \"Dog: \" + name; }\n};\n\nint main() {\n    Cat c{\"Whiskers\"};\n    Dog d{\"Rex\"};\n    c.print_info();\n    d.print_info();\n}",
          "expectedOutput": "[Info] Cat: Whiskers\n[Info] Dog: Rex",
          "explanation": "Printable не знает о Cat и Dog — у него нет шаблонного параметра. При вызове c.print_info() компилятор выводит Self = Cat и передаёт c в self. Вызов self.describe() разрешается на Cat::describe() — без downcast и без UB. При вызове d.print_info() Self выводится как Dog. Если бы какой-то наследник не определил describe(), компилятор выдал бы ошибку в точке вызова print_info(), а не в недрах шаблонного кода. Это чище классического CRTP: нет template<typename Derived> на базовом классе, нет static_cast."
        },
        {
          "id": "m2-l2-ex3",
          "title": "Overloaded для std::visit: посетитель variant без if-else",
          "level": "advanced",
          "code": "#include <iostream>\n#include <variant>\n#include <string>\n\ntemplate<typename... Ts>\nstruct Overloaded : Ts... { using Ts::operator()...; };\n\nint main() {\n    using V = std::variant<int, std::string, double>;\n    V v1 = 42;\n    V v2 = std::string(\"hello\");\n    V v3 = 3.14;\n\n    auto visitor = Overloaded{\n        [](int x)                { std::cout << \"int: \" << x << \"\\n\"; },\n        [](const std::string& s) { std::cout << \"string: \" << s << \"\\n\"; },\n        [](double d)             { std::cout << \"double: \" << d << \"\\n\"; },\n    };\n\n    std::visit(visitor, v1);\n    std::visit(visitor, v2);\n    std::visit(visitor, v3);\n}",
          "expectedOutput": "int: 42\nstring: hello\ndouble: 3.14",
          "explanation": "Overloaded наследует от всех переданных типов (в данном случае — трёх лямбд) и открывает все их operator() через using. std::visit смотрит на активный тип в variant и вызывает подходящий operator() посетителя. Компилятор проверяет, что все возможные типы variant обработаны — пропущенный вариант даст ошибку компиляции. Это безопаснее switch по std::variant::index() и читается как таблица: каждый случай — отдельная строка. Паттерн работает и без deducing this, но иллюстрирует ту же идею: агрегирование поведения без виртуальных функций и без явного downcast."
        }
      ],
      "outputsVerified": true
    },
    {
      "id": "m2-l3",
      "title": "Deducing this - Lesson 3",
      "stub": true
    }
  ]
}
