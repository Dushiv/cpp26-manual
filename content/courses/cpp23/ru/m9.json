{
  "id": "m9",
  "moduleNumber": 9,
  "title": "Числа и низкоуровневое",
  "significance": "special",
  "prerequisites": [
    "m8"
  ],
  "lessons": [
    {
      "id": "m9-l1",
      "title": "<stdfloat>: фиксированные типы с плавающей точкой",
      "prerequisites": ["m8-l2"],
      "background": null,
      "motivation": "Типы `float`, `double` и `long double` существуют в C++ со времён C, но у них есть фундаментальный изъян: стандарт никогда не гарантировал их точный размер в байтах. На большинстве x86-платформ `float` — это 32-битный IEEE 754, а `double` — 64-битный, но это лишь де-факто норма, а не требование языка. На некоторых встраиваемых и DSP-платформах картина иная. Для кода, которому важна точная семантика IEEE 754 — сетевые протоколы, форматы файлов, библиотеки машинного обучения — такая неопределённость создаёт проблемы переносимости. C++23 вводит заголовок `<stdfloat>` с пятью новыми типами с плавающей точкой фиксированного размера.",
      "theory": "До C++23 программисты, которым нужна была гарантированная точность, писали `typedef float float32_t;` — но это лишь псевдоним, не новый тип. Два псевдонима для одного и того же типа не могут служить основой для перегрузки функций, и компилятор не может отличить `float32_t` от `float`. C++23 решает эту проблему, вводя по-настоящему отдельные типы в систему типов языка.\n\nЗаголовок `<stdfloat>` определяет до пяти типов: `std::float16_t` (IEEE 754 binary16, «половинная точность»), `std::float32_t` (IEEE 754 binary32, «одинарная точность», совпадает с `float` на большинстве платформ), `std::float64_t` (IEEE 754 binary64, «двойная точность»), `std::float128_t` (IEEE 754 binary128, «четверная точность», требует аппаратной поддержки) и `std::bfloat16_t` (Brain Float 16, формат разработанный Google/Intel). Стандарт предоставляет тип только если платформа его поддерживает — на x86-64 с GCC/Clang обычно доступны все четыре, кроме `float128_t`.\n\nОсобого внимания заслуживает `bfloat16_t`. В отличие от `float16_t`, у которого 5 бит экспоненты и 10 бит мантиссы, `bfloat16_t` имеет те же 8 бит экспоненты, что и `float32_t`, но только 7 бит мантиссы. Это означает тот же диапазон значений, что у `float32_t`, при вдвое меньшем объёме памяти. Именно поэтому Google TPU, Intel AMX, TensorFlow и PyTorch используют bfloat16 для весов нейронных сетей: при обучении важна стабильность диапазона (чтобы избежать переполнения/потери значимости), а не максимальная точность мантиссы.\n\nНа практике все новые типы поддерживают арифметические операторы и работают с `std::numeric_limits<T>`. `std::format` и `std::print` форматируют их корректно. Для инициализации из `float`-литералов необходимо явное приведение типа (`static_cast`) — неявное сужение в скобочной инициализации запрещено. Ключевое преимущество перед старым подходом с `typedef`: эти типы отдельны в системе типов, что позволяет писать перегруженные функции, принимающие `float16_t` и `float32_t` как разные аргументы.",
      "examples": [
        {
          "id": "m9-l1-ex1",
          "title": "Размеры и точность новых типов",
          "description": "Проверяем размеры всех доступных типов из `<stdfloat>` и их точность через `std::numeric_limits`.",
          "code": "#include <stdfloat>\n#include <limits>\n#include <print>\n\nint main() {\n    std::println(\"sizeof float16_t:  {}\", sizeof(std::float16_t));\n    std::println(\"sizeof float32_t:  {}\", sizeof(std::float32_t));\n    std::println(\"sizeof float64_t:  {}\", sizeof(std::float64_t));\n    std::println(\"sizeof bfloat16_t: {}\", sizeof(std::bfloat16_t));\n\n    std::println(\"float32_t max digits10: {}\", std::numeric_limits<std::float32_t>::max_digits10);\n    std::println(\"float64_t max digits10: {}\", std::numeric_limits<std::float64_t>::max_digits10);\n    std::println(\"float16_t max digits10: {}\", std::numeric_limits<std::float16_t>::max_digits10);\n}",
          "expectedOutput": "sizeof float16_t:  2\nsizeof float32_t:  4\nsizeof float64_t:  8\nsizeof bfloat16_t: 2\nfloat32_t max digits10: 9\nfloat64_t max digits10: 17\nfloat16_t max digits10: 5",
          "outputsVerified": true,
          "compiler": "gsnapshot",
          "compilerFlags": "-std=c++23 -O2"
        },
        {
          "id": "m9-l1-ex2",
          "title": "bfloat16 для хранения весов нейронной сети",
          "description": "Практический сценарий из ML: хранение весов в `bfloat16_t` вместо `float32_t` вдвое сокращает память. Арифметика выполняется через явное приведение к `float`.",
          "code": "#include <stdfloat>\n#include <vector>\n#include <print>\n\nint main() {\n    // Simulate storing ML weights in bfloat16 vs float32\n    std::vector<std::bfloat16_t> bf16_weights = {\n        static_cast<std::bfloat16_t>(0.5f),\n        static_cast<std::bfloat16_t>(-0.25f),\n        static_cast<std::bfloat16_t>(1.0f),\n        static_cast<std::bfloat16_t>(0.125f)\n    };\n\n    std::vector<std::float32_t> f32_weights = {0.5f, -0.25f, 1.0f, 0.125f};\n\n    std::println(\"bfloat16 storage: {} bytes for 4 weights\",\n        bf16_weights.size() * sizeof(std::bfloat16_t));\n    std::println(\"float32 storage:  {} bytes for 4 weights\",\n        f32_weights.size() * sizeof(std::float32_t));\n\n    // Arithmetic works — convert to float32 for computation\n    float sum = 0.0f;\n    for (auto w : bf16_weights)\n        sum += static_cast<float>(w);\n    std::println(\"sum of bf16 weights: {}\", sum);\n\n    // Type system: float16_t and float32_t are distinct types\n    auto f16 = static_cast<std::float16_t>(1.5f);\n    std::float32_t f32 = 1.5f;\n    std::println(\"float16_t == float32_t value: {}\", static_cast<float>(f16) == f32);\n}",
          "expectedOutput": "bfloat16 storage: 8 bytes for 4 weights\nfloat32 storage:  16 bytes for 4 weights\nsum of bf16 weights: 1.375\nfloat16_t == float32_t value: true",
          "outputsVerified": true,
          "compiler": "gsnapshot",
          "compilerFlags": "-std=c++23 -O2"
        }
      ],
      "outputsVerified": true
    },
    {
      "id": "m9-l2",
      "title": "`std::byteswap`, `std::to_underlying`, `std::unreachable`: низкоуровневые утилиты C++23",
      "prerequisites": ["m9-l1"],
      "background": null,
      "motivation": "До C++23 для переворота байт целого числа нужно было писать `__builtin_bswap32(x)` на GCC/Clang или `_byteswap_ulong(x)` на MSVC. Чтобы пометить ветку кода как недостижимую — `__builtin_unreachable()` или `__assume(false)`. Чтобы извлечь underlying-тип из `enum class` — тяжеловесный `static_cast<std::underlying_type_t<Color>>(value)`. Каждый из этих паттернов решал реальную задачу, но с нестандартным синтаксисом: код не был переносимым и засорял кодовую базу платформо-зависимыми деталями. C++23 стандартизирует все три операции в виде чистых, `constexpr`-ready функций.",
      "theory": "Исторически C++ унаследовал от C большое число полезных низкоуровневых операций, которые оставались за пределами стандарта — каждый компилятор предоставлял собственные расширения. C++20 начал работу по стандартизации: `<bit>` получил `std::popcount`, `std::rotl`, `std::rotr`. C++23 добавляет ещё три утилиты, закрывая наиболее частые болевые точки.\n\n`std::byteswap` (заголовок `<bit>`, P1272) обращает порядок байт любого целочисленного типа. Принимает любой `integral` тип: `uint8_t`, `uint16_t`, `uint32_t`, `uint64_t`, `int`, и так далее. Возвращает тот же тип с переставленными байтами. Функция `constexpr` — вычисление возможно на этапе компиляции. Типичные сценарии: преобразование сетевого порядка байт (big-endian протокол ↔ little-endian хост), чтение бинарных форматов файлов, некоторые алгоритмы хеширования. До C++23: `__builtin_bswap32` / `__builtin_bswap64` (GCC/Clang) или `_byteswap_ulong` (MSVC), либо ручная побитовая арифметика.\n\n`std::unreachable` (заголовок `<utility>`, P0627) помечает точку кода как недостижимую. Если выполнение всё же доходит до этой точки — поведение не определено. Компилятор использует это обещание для оптимизации: может убрать проверки в `switch`-default, сократить кодовый граф, устранить мёртвые ветки. Функция особенно полезна после исчерпывающего `switch` по `enum` — позволяет избавиться от предупреждения «возможно, не все пути возвращают значение» и дать оптимизатору полную информацию. **Важно:** в отличие от `assert()`, `std::unreachable` не проверяет условие ни в debug-, ни в release-сборках. Если предположение о недостижимости ошибочно, программа получает тихое UB. Используйте только там, где вы абсолютно уверены в инварианте.\n\n`std::to_underlying` (заголовок `<utility>`, P1682) возвращает значение перечисления приведённым к его underlying-типу. Работает с `enum class` и обычным `enum`. Возвращает `std::underlying_type_t<E>` — всегда точный тип, который был объявлен как основа перечисления. Эта функция была подробно разобрана в уроке m1-l5; здесь стоит лишь отметить, что она дополняет `byteswap` и `unreachable` как третья низкоуровневая утилита C++23, переводящая многословный `static_cast<std::underlying_type_t<E>>(e)` в лаконичный вызов.",
      "examples": [
        {
          "id": "m9-l2-ex1",
          "title": "std::byteswap: преобразование порядка байт",
          "description": "Конвертация между host byte order и network byte order с помощью `std::byteswap`. Функция работает в `constexpr`-контексте — порт можно перевести ещё на этапе компиляции. `to_underlying` позволяет вывести числовой код HTTP-статуса без громоздкого `static_cast`.",
          "code": "#include <bit>\n#include <cstdint>\n#include <print>\n#include <utility>\n\nint main() {\n    // Network byte order conversion (big-endian)\n    uint32_t host_value = 0x12345678;\n    uint32_t network_value = std::byteswap(host_value);\n\n    std::println(\"host:    {:#010x}\", host_value);\n    std::println(\"network: {:#010x}\", network_value);\n    std::println(\"round-trip: {:#010x}\", std::byteswap(network_value));\n\n    // Works at compile time\n    constexpr uint16_t port = std::byteswap(uint16_t{8080});\n    std::println(\"port 8080 in network order: {:#06x}\", port);\n\n    // to_underlying\n    enum class Status { OK = 200, NotFound = 404, Error = 500 };\n    auto code = std::to_underlying(Status::NotFound);\n    std::println(\"HTTP code: {}\", code);\n}",
          "expectedOutput": "host:    0x12345678\nnetwork: 0x78563412\nround-trip: 0x12345678\nport 8080 in network order: 0x901f\nHTTP code: 404",
          "outputsVerified": true,
          "compiler": "gsnapshot",
          "compilerFlags": "-std=c++23 -O2"
        },
        {
          "id": "m9-l2-ex2",
          "title": "std::unreachable: исчерпывающий switch и оптимизатор",
          "description": "`std::unreachable()` после последнего `case` сообщает оптимизатору, что default-ветка никогда не выполнится. Это позволяет избавиться от предупреждения компилятора о «не всех путях, возвращающих значение», и помочь генератору кода.",
          "code": "#include <utility>\n#include <print>\n#include <string_view>\n\nenum class Direction { North, South, East, West };\n\nstd::string_view direction_name(Direction d) {\n    switch (d) {\n        case Direction::North: return \"North\";\n        case Direction::South: return \"South\";\n        case Direction::East:  return \"East\";\n        case Direction::West:  return \"West\";\n    }\n    std::unreachable(); // all cases handled — helps optimizer skip default\n}\n\nint opposite_sign(int x) {\n    if (x > 0) return 1;\n    if (x < 0) return -1;\n    return 0;\n    // Note: no unreachable here — 0 is a valid path\n}\n\nint main() {\n    for (auto d : {Direction::North, Direction::South, Direction::East, Direction::West})\n        std::println(\"direction: {}\", direction_name(d));\n\n    std::println(\"sign(42): {}\", opposite_sign(42));\n    std::println(\"sign(-7): {}\", opposite_sign(-7));\n    std::println(\"sign(0): {}\", opposite_sign(0));\n}",
          "expectedOutput": "direction: North\ndirection: South\ndirection: East\ndirection: West\nsign(42): 1\nsign(-7): -1\nsign(0): 0",
          "outputsVerified": true,
          "compiler": "gsnapshot",
          "compilerFlags": "-std=c++23 -O2"
        }
      ],
      "outputsVerified": true
    },
    {
      "id": "m9-l3",
      "title": "`std::out_ptr`, литералы `uz`, `ranges::shift_left/right`: финальные утилиты C++23",
      "prerequisites": ["m9-l2"],
      "background": null,
      "motivation": "Три маленьких, но практически важных добавления C++23 закрывают давние болевые точки. Суффикс `uz` наконец-то даёт тип `std::size_t` для целочисленных литералов — до этого `for (auto i = 0; i < vec.size(); ++i)` порождал предупреждение `-Wsign-compare`, а подавить его без лишних деталей было нельзя. `std::out_ptr` и `std::inout_ptr` устраняют неуклюжий двухэтапный паттерн «сначала получить сырой указатель из C API, потом завернуть в `unique_ptr`» — теперь это делается одним вызовом. `std::ranges::shift_left` и `std::ranges::shift_right` дают прямолинейный способ сдвигать элементы диапазона in-place, вместо косвенного трюка через `std::rotate` или ручного `std::move`. Каждая из трёх фич стандартизирует то, что программисты писали вручную или через нестандартные расширения годами.",
      "theory": "Суффиксы `uz` и `z` для целых литералов (P0330) решают классическую проблему: тип литерала `0` — `int`, а `.size()` возвращает `std::size_t`. Сравнение `int` и `size_t` — это сравнение знакового и беззнакового типа, что порождает предупреждение `-Wsign-compare`. До C++23 выходов было два: явно писать `size_t i = 0` или подавлять предупреждение через cast. Теперь суффикс `uz` даёт беззнаковый тип `std::size_t` (unsigned size), а `z` — знаковый `std::ptrdiff_t` (signed size). Оба работают с числами любой системы счисления: `0x10uz`, `0b1010z`. На практике `uz` используется чаще — `.size()`, `.length()`, `sizeof` все возвращают `size_t`.\n\n`std::out_ptr` и `std::inout_ptr` (заголовок `<memory>`, P1132) решают проблему взаимодействия умных указателей с C API, принимающими `T**` в качестве out-параметра. Типичный паттерн до C++23: объявить `T* raw = nullptr`, передать `&raw` в C API, а потом вручную вызвать `.reset(raw)` или заново конструировать `unique_ptr`. `std::out_ptr(ptr)` создаёт временный объект-адаптор, который хранит `T*` и при уничтожении (в конце выражения) вызывает `ptr.reset(raw_ptr)` с тем значением, которое записал C API. `std::inout_ptr` делает то же самое, но дополнительно извлекает текущий сырой указатель из умного указателя перед передачей — нужно для C API, которые могут переиспользовать уже выделенную память. Оба работают с `unique_ptr`, `shared_ptr` и любым типом, у которого есть `.reset()`.\n\n`std::ranges::shift_left` и `std::ranges::shift_right` (заголовок `<algorithm>`, P1243) сдвигают элементы диапазона на n позиций влево или вправо in-place. `shift_left(r, n)` перемещает каждый элемент на n позиций к началу: `r[i] = move(r[i+n])`. Возвращает subrange валидных элементов (первые `size - n` элементов). `shift_right(r, n)` симметрично сдвигает к концу и возвращает последние `size - n` элементов. Элементы, «вытолкнутые» за границу, остаются в диапазоне в валидном, но неопределённом состоянии — они не уничтожены, но полагаться на их значение нельзя. Размер диапазона не меняется. Если `n >= size`, возвращается пустой subrange. Полезные применения: реализация кольцевого буфера, алгоритм скользящего окна, эффективное удаление элементов с начала без `erase(begin())`, которое требует перемещения всего содержимого.\n\nВсе три добавления объединяет одна идея: устранение специфического трения, которое раньше требовало нестандартных расширений (`__builtin_bswap`, `__builtin_unreachable`), многословных паттернов (двухэтапный `T* raw; api(&raw); ptr.reset(raw)`) или обходных решений (`std::rotate` вместо прямого shift). C++23 последовательно «добивает» подобные точки — каждая маленькая утилита по отдельности кажется незначительной, но вместе они существенно снижают когнитивную нагрузку в реальном коде.",
      "examples": [
        {
          "id": "m9-l3-ex1",
          "title": "Литералы `uz`/`z` и `ranges::shift_left/right`",
          "description": "Суффикс `uz` устраняет предупреждение `-Wsign-compare` в цикле по индексу. `shift_left` и `shift_right` сдвигают элементы in-place и возвращают subrange валидных элементов.",
          "code": "#include <vector>\n#include <algorithm>\n#include <ranges>\n#include <print>\n\nint main() {\n    // uz literals eliminate sign-comparison warnings\n    std::vector<int> v = {10, 20, 30, 40, 50};\n    for (auto i = 0uz; i < v.size(); ++i)\n        std::print(\"{} \", v[i]);\n    std::println(\"\");\n\n    // shift_left: shift elements left by 2\n    std::vector<int> data = {1, 2, 3, 4, 5};\n    auto valid = std::ranges::shift_left(data, 2);\n    std::print(\"after shift_left(2), valid part: \");\n    for (int x : valid) std::print(\"{} \", x);\n    std::println(\"\");\n    std::println(\"full vector size still: {}\", data.size());\n\n    // shift_right: shift elements right by 1\n    std::vector<int> data2 = {1, 2, 3, 4, 5};\n    auto valid2 = std::ranges::shift_right(data2, 1);\n    std::print(\"after shift_right(1), valid part: \");\n    for (int x : valid2) std::print(\"{} \", x);\n    std::println(\"\");\n}",
          "expectedOutput": "10 20 30 40 50 \nafter shift_left(2), valid part: 3 4 5 \nfull vector size still: 5\nafter shift_right(1), valid part: 1 2 3 4 ",
          "outputsVerified": true,
          "compiler": "gsnapshot",
          "compilerFlags": "-std=c++23 -O2"
        },
        {
          "id": "m9-l3-ex2",
          "title": "`std::out_ptr`: умные указатели и C API",
          "description": "`std::out_ptr` заменяет двухэтапный паттерн «получить сырой указатель из C API, потом вручную вызвать `.reset()`». Адаптор сам вызывает `.reset()` при уничтожении в конце выражения.",
          "code": "#include <memory>\n#include <print>\n\n// Имитируем C API, выделяющий ресурс через out-параметр\nstruct Resource {\n    int value;\n    explicit Resource(int v) : value(v) {}\n};\n\nint c_api_create(Resource** out, int val) {\n    *out = new Resource(val);\n    return 0; // success\n}\n\nvoid c_api_destroy(Resource* r) {\n    delete r;\n}\n\nint main() {\n    // Старый паттерн (до out_ptr):\n    {\n        Resource* raw = nullptr;\n        c_api_create(&raw, 42);\n        std::unique_ptr<Resource, decltype(&c_api_destroy)> ptr{raw, c_api_destroy};\n        std::println(\"old pattern value: {}\", ptr->value);\n    }\n\n    // Новый паттерн с std::out_ptr:\n    {\n        std::unique_ptr<Resource, decltype(&c_api_destroy)> ptr{nullptr, c_api_destroy};\n        c_api_create(std::out_ptr(ptr), 99);\n        std::println(\"out_ptr pattern value: {}\", ptr->value);\n    }\n\n    std::println(\"both resources properly destroyed\");\n}",
          "expectedOutput": "old pattern value: 42\nout_ptr pattern value: 99\nboth resources properly destroyed",
          "outputsVerified": true,
          "compiler": "gsnapshot",
          "compilerFlags": "-std=c++23 -O2"
        }
      ],
      "outputsVerified": true
    }
  ]
}
