{
  "id": "m8",
  "moduleNumber": 8,
  "title": "Chrono: Calendar and Timezones",
  "significance": "basic",
  "prerequisites": ["m7"],
  "lessons": [
    {
      "id": "m8-l1",
      "title": "Date Types: year_month_day, Arithmetic, hh_mm_ss",
      "prerequisites": [],
      "background": {
        "title": "std::chrono: duration and time_point (C++11)",
        "content": "The `<chrono>` library was introduced in C++11. `std::chrono::duration<Rep, Period>` represents a time interval (e.g. `seconds`, `milliseconds`). `std::chrono::time_point<Clock, Duration>` represents a point in time relative to a clock's epoch. C++20 built a full calendar system on top of this foundation: types for year, month, day, and timezone support."
      },
      "motivation": "Before C++20, working with dates required third-party libraries (Boost.Date_Time, Howard Hinnant's date.h) or manual calculations through `time_t` / `struct tm` — with endless bugs around month-end boundaries, leap years, and timezones. C++20 brings this into the standard: the date `2026y/July/21d` reads like natural language, arithmetic (`ymd + months{3}`) handles month-end transitions automatically, and `year_month_day::ok()` validates a date instantly.",
      "theory": "## Literals and year_month_day\n\nC++20 introduces suffixes for creating date components directly in code:\n\n```cpp\nusing namespace std::chrono;\nusing namespace std::chrono_literals;\n\nauto y = 2026y;          // std::chrono::year{2026}\nauto m = July;           // std::chrono::month{7}, constant in namespace chrono\nauto d = 21d;            // std::chrono::day{21}\n```\n\n`year_month_day` stores a year, month, and day as a single value. It's conveniently created via the `/` operator:\n\n```cpp\nyear_month_day ymd = 2026y/July/21d;           // year/month/day order\nyear_month_day ymd2{year{2026}, month{7}, day{21}};  // explicit constructor\n```\n\nFormatting uses `std::format` with chrono format specifiers:\n\n```cpp\nstd::format(\"{:%Y-%m-%d}\", ymd)   // \"2026-07-21\"\nstd::format(\"{:%d %B %Y}\", ymd)   // \"21 July 2026\"\n```\n\n## Date Arithmetic\n\nYou can add `months` and `years` to `year_month_day`; the result correctly handles month-end transitions:\n\n```cpp\nauto ymd = 2026y/July/21d;\nauto next_month = ymd + months{1};   // 2026-08-21\nauto next_year  = ymd + years{1};    // 2027-07-21\n```\n\nFor day-level arithmetic, convert through `sys_days`:\n\n```cpp\nsys_days sd{ymd};              // time_point<system_clock, days>\nauto tomorrow = sd + days{1};  // arithmetic works on days\nyear_month_day tomorrow_ymd{tomorrow};  // convert back to calendar type\n```\n\n## Last Day of Month and weekday\n\n`std::chrono::last` is a special marker for \"the last day of the month\":\n\n```cpp\nauto feb_last = 2026y/February/last;       // year_month_day_last\nyear_month_day feb_last_ymd{feb_last};     // 2026-02-28\n```\n\n`std::chrono::weekday` represents a day of the week (0 = Sunday, 6 = Saturday). Constants `Sunday`…`Saturday` are defined in namespace `std::chrono`:\n\n```cpp\nsys_days sd{2026y/July/21d};\nweekday wd{sd};               // computed from date -> sys_days\nstd::format(\"{}\", wd);        // \"Tue\"\n```\n\n## Validation: ymd.ok()\n\n`year_month_day::ok()` returns `true` if the date exists in the real calendar:\n\n```cpp\nyear_month_day valid{2026y/July/21d};       // ok() == true\nyear_month_day invalid{2026y/February/30d}; // ok() == false (Feb has no 30th)\n```\n\nThis is useful when parsing user input — create the object and check immediately.\n\n## hh_mm_ss: Breaking Down a Duration\n\n`std::chrono::hh_mm_ss<Duration>` splits any `duration` into hours, minutes, seconds, and subseconds:\n\n```cpp\nauto d = 3h + 45min + 30s + 123ms;\nhh_mm_ss hms{d};\nhms.hours().count()      // 3\nhms.minutes().count()    // 45\nhms.seconds().count()    // 30\nhms.subseconds().count() // 123  (type is milliseconds because the input Duration is ms)\n```\n\nThe subsecond type is automatically derived from the precision of the input `Duration`. Pass `nanoseconds` and `subseconds()` will return `nanoseconds`.",
      "examples": [
        {
          "id": "m8-l1-ex1",
          "title": "Creating dates, arithmetic, and validation",
          "code": "#include <chrono>\n#include <iostream>\n#include <format>\n\nint main() {\n    using namespace std::chrono;\n    using namespace std::chrono_literals;\n\n    // Two ways to create the same date\n    year_month_day today{year{2026}, month{7}, day{21}};\n    auto d2 = 2026y/July/21d;  // same date via literals\n\n    std::cout << std::format(\"{:%Y-%m-%d}\", today) << \"\\n\";  // ISO format\n    std::cout << std::format(\"{:%d %B %Y}\", d2) << \"\\n\";     // textual month\n\n    // Date arithmetic\n    auto next_month = today + months{1};\n    auto next_year  = today + years{1};\n    std::cout << std::format(\"{:%Y-%m-%d}\", next_month) << \"\\n\";\n    std::cout << std::format(\"{:%Y-%m-%d}\", next_year) << \"\\n\";\n\n    // Last day of February 2026\n    auto feb_last = 2026y/February/last;\n    std::cout << std::format(\"{:%Y-%m-%d}\", year_month_day{feb_last}) << \"\\n\";\n\n    // Day of week\n    sys_days sd{today};\n    weekday wd{sd};\n    std::cout << std::format(\"{}\", wd) << \"\\n\";\n\n    // Validation\n    year_month_day invalid{year{2026}, month{2}, day{30}};\n    std::cout << \"2026-02-30 valid: \" << invalid.ok() << \"\\n\";\n    std::cout << \"2026-07-21 valid: \" << today.ok() << \"\\n\";\n}",
          "expectedOutput": "2026-07-21\n21 July 2026\n2026-08-21\n2027-07-21\n2026-02-28\nTue\n2026-02-30 valid: 0\n2026-07-21 valid: 1"
        },
        {
          "id": "m8-l1-ex2",
          "title": "hh_mm_ss and converting between date types",
          "code": "#include <chrono>\n#include <iostream>\n#include <format>\n\nint main() {\n    using namespace std::chrono;\n    using namespace std::chrono_literals;\n\n    // hh_mm_ss splits a duration into components\n    auto duration = 3h + 45min + 30s + 123ms;\n    hh_mm_ss hms{duration};\n    std::cout << hms.hours().count() << \"h \"\n              << hms.minutes().count() << \"m \"\n              << hms.seconds().count() << \"s \"\n              << hms.subseconds().count() << \"ms\\n\";\n\n    // sys_days -> year_month_day: day 100 from January 1, 2026\n    sys_days sd = sys_days{year_month_day{2026y/January/1d}};\n    year_month_day ymd{sd + days{100}};\n    std::cout << std::format(\"{:%Y-%m-%d}\", ymd) << \"\\n\";  // 2026-04-11\n\n    // Days since the Unix epoch (1970-01-01)\n    auto epoch_days = sd.time_since_epoch().count();\n    std::cout << \"days since epoch: \" << epoch_days << \"\\n\";\n}",
          "expectedOutput": "3h 45m 30s 123ms\n2026-04-11\ndays since epoch: 20454"
        }
      ],
      "outputsVerified": true
    },
    {
      "id": "m8-l2",
      "title": "Timezones: time_zone, zoned_time, Conversion",
      "prerequisites": ["m8-l1"],
      "background": null,
      "motivation": "Without standard timezone support, developers either hardcoded UTC offsets or pulled in third-party libraries that could go stale as the IANA database evolved. C++20 includes full IANA timezone database integration: a single call to `locate_zone(\"America/New_York\")` gives you an object that knows the complete DST transition history. `zoned_time` stores a time point paired with a timezone and automatically converts between them when formatting.",
      "theory": "## The IANA Database and get_tzdb()\n\nC++20 requires implementations to provide access to the IANA (Internet Assigned Numbers Authority) timezone database — the same one used in Linux's `/usr/share/zoneinfo`. Access it via:\n\n```cpp\nconst auto& db = std::chrono::get_tzdb();  // tzdb&\nstd::cout << db.version;  // e.g. \"2026b\"\n```\n\n`get_tzdb()` returns a reference to a global singleton containing all timezone definitions and their historical UTC offset changes.\n\n## locate_zone and time_zone\n\n```cpp\nconst std::chrono::time_zone* tz = std::chrono::locate_zone(\"Europe/Berlin\");\n```\n\nIf the name is not found, `std::runtime_error` is thrown. Name strings follow IANA conventions: \"America/New_York\", \"Asia/Tokyo\", \"UTC\", \"Europe/Berlin\", etc.\n\n## zoned_time\n\n`std::chrono::zoned_time<Duration>` is a pair: a system time (`sys_time<Duration>`) plus a pointer to a `time_zone`. Construct it from a zone and a `sys_time`:\n\n```cpp\nusing namespace std::chrono;\nsys_days sd{year{2026}/March/8d};\nauto tp = sd + hours{12};           // 2026-03-08 12:00:00 UTC\n\nconst time_zone* utc = locate_zone(\"UTC\");\nzoned_time<seconds> zt{utc, tp};    // bind the moment to a timezone\n```\n\nWhen formatting, `%Z` gives the timezone abbreviation and `%H:%M:%S` gives the local time:\n\n```cpp\nstd::format(\"{:%Y-%m-%d %H:%M:%S %Z}\", zt)  // \"2026-03-08 12:00:00 UTC\"\n```\n\nConverting between zones is simply constructing a new `zoned_time` from an existing one:\n\n```cpp\nconst time_zone* ny = locate_zone(\"America/New_York\");\nzoned_time<seconds> zt_ny{ny, zt};  // same moment, different timezone\n```\n\n## Daylight Saving Time (DST)\n\n`zoned_time` handles DST transitions automatically. On March 8, 2026, US clocks spring forward: 2:00 AM → 3:00 AM. The moment 6:30 UTC = 1:30 EST (before transition); 8:00 UTC = 4:00 EDT (after transition):\n\n```cpp\n// 6:30 UTC -> 1:30 EST (Eastern Standard Time, UTC-5)\nzoned_time<minutes> before{ny, sd + hours{6} + minutes{30}};\n// 8:00 UTC -> 4:00 EDT (Eastern Daylight Time, UTC-4)\nzoned_time<minutes> after{ny, sd + hours{8}};\n```\n\nThe `%Z` abbreviation changes automatically: EST before the transition, EDT after.\n\n## clock_cast: Converting Between Clock Types\n\nC++20 introduces several system clocks with different epochs:\n\n| Type | Epoch | Notes |\n|------|-------|-------|\n| `system_clock` | 1970-01-01 UTC | Can jump (NTP adjustments) |\n| `utc_clock` | 1970-01-01 UTC | Counts leap seconds |\n| `tai_clock` | 1958-01-01 | International Atomic Time |\n| `gps_clock` | 1980-01-06 UTC | GPS time |\n\n`clock_cast<TargetClock>(tp)` converts between them:\n\n```cpp\nsys_seconds sys_tp = /*...*/;\nutc_seconds utc_tp = clock_cast<utc_clock>(sys_tp);\ngps_seconds gps_tp = clock_cast<gps_clock>(sys_tp);\n\n// utc_clock and system_clock differ by accumulated leap seconds\n// (27 as of 2026)\n```",
      "examples": [
        {
          "id": "m8-l2-ex1",
          "title": "Timezones: locate_zone, zoned_time, formatting",
          "code": "#include <chrono>\n#include <iostream>\n#include <format>\n\nint main() {\n    using namespace std::chrono;\n\n    // IANA timezone database version\n    const auto& tzdb = get_tzdb();\n    std::cout << \"tzdb version: \" << tzdb.version << \"\\n\";\n\n    // UTC is always available\n    const time_zone* utc = locate_zone(\"UTC\");\n\n    // Fixed moment: 2026-03-08 12:00:00 UTC\n    sys_days sd{year{2026}/March/8d};\n    auto tp = sd + hours{12};\n\n    zoned_time<seconds> zt_utc{utc, tp};\n    std::cout << std::format(\"{:%Y-%m-%d %H:%M:%S %Z}\", zt_utc) << \"\\n\";\n\n    // Same moment in New York\n    const time_zone* ny = locate_zone(\"America/New_York\");\n    zoned_time<seconds> zt_ny{ny, tp};\n    std::cout << std::format(\"{:%Y-%m-%d %H:%M:%S %Z}\", zt_ny) << \"\\n\";\n\n    // Convert to Tokyo\n    const time_zone* tokyo = locate_zone(\"Asia/Tokyo\");\n    zoned_time<seconds> zt_tokyo{tokyo, zt_utc};\n    std::cout << std::format(\"{:%Y-%m-%d %H:%M:%S %Z}\", zt_tokyo) << \"\\n\";\n}",
          "expectedOutput": "tzdb version: 2026b\n2026-03-08 12:00:00 UTC\n2026-03-08 08:00:00 EDT\n2026-03-08 21:00:00 JST"
        },
        {
          "id": "m8-l2-ex2",
          "title": "DST transitions and clock_cast",
          "code": "#include <chrono>\n#include <iostream>\n#include <format>\n\nint main() {\n    using namespace std::chrono;\n\n    // March 8, 2026: US clocks spring forward (2:00 AM -> 3:00 AM)\n    const time_zone* ny = locate_zone(\"America/New_York\");\n    sys_days sd{year{2026}/March/8d};\n\n    // 6:30 UTC = 1:30 EST (before transition)\n    auto before = sd + hours{6} + minutes{30};\n    zoned_time<minutes> zt_before{ny, before};\n    std::cout << \"before DST: \" << std::format(\"{:%H:%M %Z}\", zt_before) << \"\\n\";\n\n    // 8:00 UTC = 4:00 EDT (after transition)\n    auto after = sd + hours{8};\n    zoned_time<minutes> zt_after{ny, after};\n    std::cout << \"after DST:  \" << std::format(\"{:%H:%M %Z}\", zt_after) << \"\\n\";\n\n    // clock_cast: convert between clock types\n    sys_seconds sys_tp{sd + hours{12}};\n    utc_seconds utc_tp = clock_cast<utc_clock>(sys_tp);\n    gps_seconds gps_tp = clock_cast<gps_clock>(sys_tp);\n\n    // utc_clock accounts for leap seconds\n    auto utc_sys_diff = utc_tp.time_since_epoch().count()\n                      - sys_tp.time_since_epoch().count();\n    std::cout << \"UTC - system_clock diff: \" << utc_sys_diff << \" s (leap seconds)\\n\";\n    std::cout << \"GPS time_since_epoch: \" << gps_tp.time_since_epoch().count() << \" s\\n\";\n}",
          "expectedOutput": "before DST: 01:30 EST\nafter DST:  04:00 EDT\nUTC - system_clock diff: 27 s (leap seconds)\nGPS time_since_epoch: 1457006418 s"
        }
      ],
      "outputsVerified": true
    },
    {
      "id": "m8-l3",
      "title": "chrono in Practice: Measurement, parse, Formatting",
      "prerequisites": ["m8-l2"],
      "background": null,
      "motivation": "Knowing the chrono types is only half the battle. Real code needs to: measure execution time (while choosing the right clock), parse a date from a string, find the difference between two dates, and determine the day of the week for an arbitrary date. C++20 closes the last gap: `std::chrono::parse` allows parsing a string into any chrono type using the same format specifiers that `std::format` uses for output.",
      "theory": "## system_clock vs steady_clock\n\nC++20 provides two primary clocks for different purposes:\n\n| Clock | Monotonic | When to use |\n|-------|-----------|-------------|\n| `system_clock` | No (can jump during NTP) | Timestamps, logs, calendar dates |\n| `steady_clock` | Yes (always forward) | Measuring elapsed time |\n\nAlways use `steady_clock` for benchmarks:\n\n```cpp\nauto t0 = std::chrono::steady_clock::now();\n// ... code to measure ...\nauto t1 = std::chrono::steady_clock::now();\nauto elapsed = std::chrono::duration_cast<std::chrono::microseconds>(t1 - t0);\n```\n\nFor obtaining the current calendar date, use `system_clock`:\n\n```cpp\nauto now = std::chrono::system_clock::now();\nstd::chrono::year_month_day today{std::chrono::floor<std::chrono::days>(now)};\n```\n\n## duration_cast, floor, ceil, round\n\nConverting a `duration` to a different unit:\n\n```cpp\nstd::chrono::milliseconds ms{1750};\n\nduration_cast<seconds>(ms)  // 1 s (truncates toward zero)\nfloor<seconds>(ms)          // 1 s (toward negative infinity)\nceil<seconds>(ms)           // 2 s (toward positive infinity)\nround<seconds>(ms)          // 2 s (to nearest; ties go to even)\n```\n\nFor non-negative values, `duration_cast` and `floor` produce the same result. The difference appears with negative values.\n\n## Parsing Dates: std::chrono::parse\n\nC++20 adds `std::chrono::parse` — the inverse of `std::format`. It's used via `operator>>` with a proxy object:\n\n```cpp\n#include <sstream>\n\nstd::istringstream ss{\"2026-12-31\"};\nstd::chrono::year_month_day ymd;\nss >> std::chrono::parse(\"%Y-%m-%d\", ymd);\n// ymd == 2026y/December/31d\n```\n\nSame specifiers as `std::format`: `%Y` = year, `%m` = month as number, `%d` = day, `%H:%M:%S` = time. You can parse directly into `sys_seconds`, `sys_days`, `year_month_day`, and other chrono types.\n\nIf the string doesn't match the format, the stream enters an error state (`failbit`).\n\n## Practical Date Calculations\n\nDifference between two dates in days:\n\n```cpp\nusing namespace std::chrono;\nint days_between(year_month_day from, year_month_day to) {\n    return (sys_days{to} - sys_days{from}).count();\n}\n```\n\nDay of the week for an arbitrary date:\n\n```cpp\nyear_month_day date{2026y/December/25d};\nweekday wd{sys_days{date}};\nstd::format(\"{}\", wd);  // \"Fri\"\n```\n\nNotice the pattern: `year_month_day` → `sys_days` is almost always the intermediate step for any computation, because `sys_days` is simply a `time_point<system_clock, days>` on which all `duration` arithmetic operates.",
      "examples": [
        {
          "id": "m8-l3-ex1",
          "title": "duration_cast, floor/ceil/round, date arithmetic",
          "code": "#include <chrono>\n#include <iostream>\n#include <format>\n\nint main() {\n    using namespace std::chrono;\n\n    // floor, ceil, round for duration\n    milliseconds ms{1750};\n    std::cout << \"floor to seconds: \" << floor<seconds>(ms).count() << \"\\n\";\n    std::cout << \"ceil  to seconds: \" << ceil<seconds>(ms).count() << \"\\n\";\n    std::cout << \"round to seconds: \" << round<seconds>(ms).count() << \"\\n\";\n\n    // Duration arithmetic\n    auto d = hours{2} + minutes{30} + seconds{15};\n    std::cout << \"total seconds: \" << duration_cast<seconds>(d).count() << \"\\n\";\n    std::cout << \"total minutes: \" << duration_cast<minutes>(d).count() << \"\\n\";\n\n    // Day 180 from January 1, 2026 at 9:00 UTC\n    sys_days epoch{year{2026}/January/1d};\n    auto event = epoch + days{180} + hours{9};\n    year_month_day event_ymd{floor<days>(event)};\n    std::cout << std::format(\"event date: {:%Y-%m-%d}\", event_ymd) << \"\\n\";\n\n    // How many days from Jan 1 to the event?\n    auto days_count = duration_cast<days>(event - epoch).count();\n    std::cout << \"days since Jan 1: \" << days_count << \"\\n\";\n\n    // Day of the week for the event\n    weekday wd{floor<days>(event)};\n    std::cout << std::format(\"weekday: {}\", wd) << \"\\n\";\n}",
          "expectedOutput": "floor to seconds: 1\nceil  to seconds: 2\nround to seconds: 2\ntotal seconds: 9015\ntotal minutes: 150\nevent date: 2026-06-30\ndays since Jan 1: 180\nweekday: Tue"
        },
        {
          "id": "m8-l3-ex2",
          "title": "Parsing dates and practical helper functions",
          "code": "#include <chrono>\n#include <iostream>\n#include <format>\n#include <sstream>\n\n// Number of days between two dates\nint days_until(std::chrono::year_month_day from, std::chrono::year_month_day to) {\n    using namespace std::chrono;\n    return (sys_days{to} - sys_days{from}).count();\n}\n\nint main() {\n    using namespace std::chrono;\n    using namespace std::chrono_literals;\n\n    // Parse a date from a string\n    std::istringstream ss1{\"2026-12-31\"};\n    year_month_day new_year_eve;\n    ss1 >> parse(\"%Y-%m-%d\", new_year_eve);\n    std::cout << std::format(\"parsed: {:%Y-%m-%d}\", new_year_eve) << \"\\n\";\n\n    // Parse date and time together\n    std::istringstream ss2{\"2026-07-21 14:30:00\"};\n    sys_seconds tp;\n    ss2 >> parse(\"%Y-%m-%d %H:%M:%S\", tp);\n    std::cout << std::format(\"parsed datetime: {:%Y-%m-%d %H:%M:%S}\", tp) << \"\\n\";\n\n    // How many days until December 31?\n    year_month_day today{year{2026}/July/21d};\n    int d = days_until(today, new_year_eve);\n    std::cout << \"days until Dec 31: \" << d << \"\\n\";\n\n    // What day of the week is Christmas 2026?\n    year_month_day christmas{year{2026}/December/25d};\n    weekday wd{sys_days{christmas}};\n    std::cout << std::format(\"Christmas 2026: {}\", wd) << \"\\n\";\n\n    // steady_clock for benchmarks (monotonic, never jumps)\n    auto t0 = steady_clock::now();\n    volatile long s = 0;\n    for (int i = 0; i < 100000; ++i) s += i;\n    auto t1 = steady_clock::now();\n    auto us = duration_cast<microseconds>(t1 - t0).count();\n    std::cout << \"benchmark: \" << (us > 0 ? \"measured\" : \"instant\") << \"\\n\";\n}",
          "expectedOutput": "parsed: 2026-12-31\nparsed datetime: 2026-07-21 14:30:00\ndays until Dec 31: 163\nChristmas 2026: Fri\nbenchmark: measured"
        }
      ],
      "outputsVerified": true
    }
  ]
}
